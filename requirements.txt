# Runtime dependencies. Only two, deliberately - no numpy, which would add
# about 45 MB to the frozen build for work Pillow already does.
#
# Last built and tested against pymupdf 1.28.2 and pillow 12.3.0.
pymupdf>=1.23.8
pillow>=10.0.1
